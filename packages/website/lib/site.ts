/** Links and copy shared by the landing page and the docs shell. */
export const site = {
  name: "@wych/react",
  /** The headline: the promise. */
  tagline: "React features as pure reducers. Side effects as Effect programs.",
  /** The subline: the mechanism. */
  subline:
    "Typed, cancellable, injectable async, testable to quiescence without a renderer. The Elm architecture for React, with Effect doing the effects.",
  /** One-line description for metadata and package listings. */
  description: "A TEA-style feature runtime for React, built on Effect.",
  github: "https://github.com/stefvw93/wych",
  npm: "https://www.npmjs.com/package/@wych/react",
  install: "npm install @wych/react effect react react-dom",
} as const;
